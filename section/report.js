// inyectar botones para mostrar comidas desde JS al HTML
/* const contenedor1 = document.querySelector('#formulario');
const btnAgregar = document.querySelector('#agregar'); */

//Variable para el total de elementos agregados
/* let total = 1; */

//Metodo que se ejecuta cuando se da click al boton de agregar elementos

/* btnAgregar.addEventListener('click', e =>{
    let div = document.createElement('div');
    div.innerHTML = ` 
     <label>${total++}</label> - <input type="text" name "nombre[]" 
     placeholder="escribe tu comida" required>
     <button class="btn btn-danger" 
     onclick="eliminar(this)">Eliminar</button>
   `;
    
  contenedor.appendChild(div);
})  */

// Metodo para eliminar el div contenedor del input
// param {this} e

/*  const eliminar = (e) => {
    const divPadre = e.parentNode;
    contenedor1.removeChild(divPadre);
    actualizarContador();
};  */

//Metodo ara actualizar el contador de los elementos agregados

/*  const actualizarContador = () => {
    let divs = contenedor1.children;
    total = 1;
    for (let i = 0; i < divs.length; i++){
        divs[i].children[0].innerHTML = total++;
    }
} 
 */


